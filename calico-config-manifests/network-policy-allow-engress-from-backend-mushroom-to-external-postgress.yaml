apiVersion: projectcalico.org/v3
kind: NetworkPolicy
metadata:
  name: allow-egress-external-postgres-db
  namespace: backend
spec:
  selector: app.kubernetes.io/name == 'mushroom-api'
  types:
    - Egress
  egress:
    - action: Allow
      destination:
        nets:
          - 192.168.68.103/32
        port:
          - 5432